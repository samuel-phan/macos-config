[project]
name = "macos-config"
version = "0.1.0"
description = "macOS configuration"
readme = "README.md"
requires-python = ">=3.14.7"
dependencies = [
    "ansible>=14.3.1",
    "ansible-lint>=26.4.0",
]
